# docs/ARCHITECTURE.md
# 填写指引：核心价值不在于描述系统是什么，而在于描述"为什么这么设计"和"什么是明确禁止的"。
# "既有规则又有理由"的写法比单纯写规则强 10 倍——Agent 理解理由后才能在边缘情况下做出正确判断。
# 写完后删除所有以 # 开头的注释行。

## 系统边界（数据流向）
# 用文字描述数据从哪里来、经过哪些层、到哪里去。
# 然后明确列出禁止的数据流。
[用户入口] → [API 层] → [服务层] → [数据层]

禁止：[层A] 直接操作 [层C]（跳过中间层）
禁止：[层A] 直接依赖 [层B 的内部实现]

## 模块职责与禁止行为

### [模块名，如 api/]
职责：[这个模块负责什么]
禁止：
- 不得直接调用 [禁止的依赖]
- 不得包含 [禁止的逻辑，如业务逻辑/DB 操作]

### [模块名，如 services/]
职责：[这个模块负责什么]
禁止：
- 不导入任何 HTTP 框架依赖（FastAPI/Starlette/Flask 等）
- 不直接读取环境变量（通过 config.py 注入）
- 不抛出 HTTP 异常（抛 Domain Exception，由 router 层转译）

### [模块名，如 models/]
职责：[这个模块负责什么]
禁止：
- 不包含业务逻辑
- 不依赖其他模块

## 禁止依赖矩阵
# ✓ = 允许，✗ = 绝对禁止
#
#              → api/   services/  models/   db/
# api/              -       ✓         ✓       ✗
# services/         ✗       -         ✓       ✓
# models/           ✗       ✗         -       ✗
# db/               ✗       ✗         ✓       -
#
# [根据你的项目调整上面的矩阵]

## 关键架构决策
# 每个重要决策写：决定 + 原因。有原因的规则 Agent 才能在边缘情况下判断。

1. [决策描述]
   原因：[为什么这么设计]

2. 所有 API 响应必须通过 Schema 验证（Pydantic / Zod）
   原因：类型安全 + 自动文档 + 防止错误数据传播

3. [其他关键决策]
   原因：[理由]

## 已知技术债
# 记录有意识的 shortcuts，防止 Agent 以为这是正确模式
- [描述已知的技术债和计划的修复时间]
